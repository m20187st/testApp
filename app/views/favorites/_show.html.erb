<% if user_signed_in? %>
	<% if post.favorited_by?(current_user)%>
	<%#<%= button_to "❤︎", post_favorite_path(id: post.favorites), method: :delete %>
	<%= button_to "❤︎", post_favorite_path(post, id: post.favorites), method: :delete%>
	<% else %>
	<%= button_to "♡", post_favorites_path(post), method: :post %>
	<% end %>
<% end %>

<%# show action は Favoriteを持ってきてるから動いたのか？ %>

<%if false %>
<% if user_signed_in? %>
	<% if post.favorited_by?(current_user)%>
	<%= button_to "❤︎", post_favorite_path(post.id, id: post.favorites), method: :delete%>
	<% else %>
	<%= button_to "♡", post_favorites_path(post), method: :post %>
	<% end %>
<% end %>
<% end%>
